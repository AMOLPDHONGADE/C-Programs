// if - Else conditional loop use 
#include<stdio.h>

void main(){
	int a=20;        // taken the value of a as 20
	int b=25;        // taken the value of b as 25
	
	if (a<b){   // if a is gretaer than b then "/print1"/ will be printed.
		printf("the value of a is less than the value of b");  //print 1
	}
	else{     // if b is gretaer than a then "/print2"/ will be printed.
		printf("the value if b is greater than the value of a"); //print 2
	}
}
